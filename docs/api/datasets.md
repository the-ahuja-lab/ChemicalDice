# Datasets Compression APIs

```python
from ChemicalDice.training.prepare_data import format_dataset_pipeline

def format_dataset_pipeline(input_dir: str, output_dir: str)
```
Sequences unstructured floating variables linearly to tightly compacted `.h5` objects natively optimizing PyTorch extraction maps.

## `process_mordred_with_imputation`
Dynamically hooks pure SkLearn KNN variables tracking $k=5$ Euclidean neighborhoods dynamically masking NaN geometries caused by disconnected 2D graphs.
