# Descriptors

```python
from ChemicalDice.training.prepare_data import generate_multi_descriptors

def generate_multi_descriptors(input_csv: str, output_dir: str = "Chemicaldice_data")
```

Encapsulates global generation topologies mathematically mapping 5 distinct modal paradigms:
- Triggers ImageMol array conversions natively capturing visual representations.
- Extrapolates Signaturizer and Chemberta mappings deterministically.
