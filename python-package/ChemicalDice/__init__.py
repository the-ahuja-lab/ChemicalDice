"""
ChemicalDice Integrator API
===========================

The primary entry point to the minimal feature extraction package.
"""

__version__ = "1.0.0"

# Note: Lazy imports are enforced aggressively in sub-packages
# Core functionality depends only on Tier 1 (numpy, pandas, requests, rdkit).
